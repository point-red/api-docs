get:
  tags:
    - KPI Template
  summary: KPI Template - Detail
  parameters:
    - name: id
      in: path
      required: true
      description: kpi tempalte id
      example: 2
  responses:
    '200':
      description: OK
      content:
        application/json:
          schema:
            type: object
            properties:
              data:
                $ref: '../components/properties/templates/template-single.yaml'
          example:
            $ref: '../components/example/templates/template.yaml'
patch:
  tags:
  - KPI Template
  summary: KPI Template - Update
  parameters:
    - name: id
      in: path
      required: true
      schema:
        type: string
      description: kpi tempalte id
      example: 2
  requestBody:
    content:
      application/json:
        schema:
          type: object
          required:
            - name
          properties:
            name:
              type: string
          example:
            name: template
  responses:
    '200':
      description: Ok
      content:
        application/json:
          schema:
            type: object
            properties:
              data:
                $ref: '../components/properties/templates/template-single.yaml'
          example:
            $ref: '../components/example/templates/template.yaml'
delete:
  tags:
    - KPI Template
  summary: KPI Template - Delete
  parameters:
    - name: id
      in: path
      required: true
      description: kpi tempalte id
      example: 2
  responses:
    '200':
      description: Successful response
      content:
        application/json: {}
put:
  tags:
    - KPI Template
  summary: KPI Template - Archive
  parameters:
    - name: id
      in: path
      required: true
      description: kpi tempalte id
      example: 2
  responses:
    '200':
      description: Successful response
      content:
        application/json:
          schema:
            type: object
            properties:
              data:
                $ref: '../components/properties/templates/template-single.yaml'
          example:
            $ref: '../components/example/templates/template.yaml'