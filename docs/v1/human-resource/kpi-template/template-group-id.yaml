get:
  tags:
    - KPI Template Group
  summary: KPI Template Group - Detail
  parameters:
    - name: id
      in: path
      required: true
      description: kpi tempalte group id
      example: 2
  responses:
    '200':
      description: OK
      content:
        application/json:
          schema:
            type: object
            properties:
              data:
                $ref: '../components/properties/templates/group-single.yaml'
          example:
            $ref: '../components/example/templates/group.yaml'
patch:
  tags:
  - KPI Template Group
  summary: KPI Template Group - Update
  parameters:
    - name: id
      in: path
      required: true
      schema:
        type: string
      description: kpi tempalte group id
      example: 2
  requestBody:
    content:
      application/json:
        schema:
          type: object
          required:
            - name
            - kpi_template_id
          properties:
            name:
              type: string
            kpi_template_id:
              type: integer
          example:
            name: template
            kpi_template_id: 1
  responses:
    '200':
      description: Ok
      content:
        application/json:
          schema:
            type: object
            properties:
              data:
                $ref: '../components/properties/templates/group-single.yaml'
          example:
            $ref: '../components/example/templates/group.yaml'
delete:
  tags:
    - KPI Template Group
  summary: KPI Template Group - Delete
  parameters:
    - name: id
      in: path
      required: true
      description: kpi tempalte group id
      example: 2
  responses:
    '200':
      description: Successful response
      content:
        application/json: {}
